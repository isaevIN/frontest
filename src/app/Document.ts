export class Document{
    id: number;
    typeDocument: String;

    constructor()
    constructor(
    id?: number, 
    typeDocument?: String, 
) {
    this.id = id
    this.typeDocument = typeDocument
  }
}